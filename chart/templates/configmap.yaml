apiVersion: v1
kind: ConfigMap

metadata:
  name: {{ include "kleister-api.fullname" . }}-providers
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "kleister-api.labels" . | nindent 4 }}

data:
  config.yaml: |
    providers:
      {{- toYaml .Values.auth.providers | nindent 6 }}
